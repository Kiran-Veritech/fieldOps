VITE_API_URL=http://localhost:8000

# Optional: force a custom dark MapLibre style URL (overrides Dark basemap only).
# Light / Satellite stay built-in. Default Dark = CARTO Dark Matter (no API key).
# VITE_MAP_STYLE_URL=https://basemaps.cartocdn.com/gl/dark-matter-gl-style/style.json
